name: "CodeQL Advanced Security Scan"

on:
  push:
    branches: [ "main", "v2.1" ]
  pull_request:
    branches: [ "main", "v2.1" ]
  schedule:
    - cron: '0 0 * * 0'   # Weekly

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
      packages: read

    strategy:
      fail-fast: false
      matrix:
        language: [ 'javascript', 'python', 'rust', 'actions' ]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v3
        with:
          languages: ${{ matrix.language }}
          # Enhanced configuration for PiRC (fixes JS/TS syntax errors)
          config: |
            paths-ignore:
              - 'node_modules/**'
              - 'dist/**'
              - 'build/**'
              - '.next/**'
              - 'frontend/node_modules/**'
              - 'public/**'
              - 'PiRC-*/**'
              - 'automation/**'
              - 'deploy/**'
              - 'enhanced/**'
              - 'pirc2_integration/**'
              - 'run/**'
              - '**/*.min.js'
              - '**/*.bundle.js'
            queries: security-extended, security-and-quality
            # For JavaScript/TypeScript: disable autobuild if syntax issues persist
            build-mode: ${{ matrix.language == 'javascript' && 'none' || 'auto' }}

      - name: Autobuild (Rust & Python only)
        if: matrix.language != 'javascript' && matrix.language != 'actions'
        uses: github/codeql-action/autobuild@v3

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: "/language:${{ matrix.language }}"
          upload: true
