[package]
name = "rwa_verify"
version = "0.1.0"
edition = "2021"

[dependencies]
soroban-sdk = "26.1.0"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
soroban-sdk = { version = "26.1.0", features = ["testutils"] }
